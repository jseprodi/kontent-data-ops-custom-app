# Environment Variables for Kontent.ai Data-Ops Custom App
# Copy this file to .env and fill in your values

# Server Configuration
# Port for the development server (default: 3000)
PORT=3000

# Node Environment
# Set to 'production' when deploying (disables CORS and static file serving)
NODE_ENV=development

# Data-Ops CLI Configuration
# Path to the data-ops CLI executable (optional)
# Defaults to: ./data-ops/build/src/index.js
# Set this if you have data-ops installed globally or in a different location
DATA_OPS_CLI_PATH=

# Security Notes:
# - API keys should NEVER be stored in environment variables for this app
# - API keys are entered by users in the UI and stored in browser localStorage
# - The server does not store or log API keys
# - For production deployment, ensure proper security measures are in place
